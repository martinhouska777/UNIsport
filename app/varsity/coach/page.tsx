"use client";

/*
  The Coach Console opens on TODAY — the morning on one screen — rather than
  on the Plan tab's list of blocks. See components/varsity/coach/today.
*/
import CoachTodayScreen from "@/components/varsity/coach/today/CoachTodayScreen";

export default function CoachIndex() {
  return <CoachTodayScreen />;
}
